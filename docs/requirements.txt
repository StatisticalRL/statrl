# Documentation build dependencies (the statrl package + its runtime deps are installed
# separately via `pip install .`).
sphinx>=9.1.0
pydata-sphinx-theme>=0.21.0
myst-parser>=5.1.0
sphinx-design>=0.7.0
